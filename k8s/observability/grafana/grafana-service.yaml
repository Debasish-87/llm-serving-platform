apiVersion: v1
kind: Service

metadata:
  name: grafana
  namespace: llm-serving

spec:
  type: ClusterIP

  selector:
    app: grafana

  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
