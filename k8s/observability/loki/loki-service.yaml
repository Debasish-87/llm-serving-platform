apiVersion: v1
kind: Service

metadata:
  name: loki
  namespace: llm-serving

spec:
  type: ClusterIP

  selector:
    app: loki

  ports:
    - name: http
      protocol: TCP
      port: 3100
      targetPort: http
