apiVersion: v1
kind: Service

metadata:
  name: alertmanager
  namespace: llm-serving
  labels:
    app: alertmanager

spec:
  type: ClusterIP

  selector:
    app: alertmanager

  ports:
    - name: http
      protocol: TCP
      port: 9093
      targetPort: http
