apiVersion: v1
kind: Service

metadata:
  name: llm-gateway
  namespace: llm-serving

spec:
  type: ClusterIP

  selector:
    app: llm-gateway

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
